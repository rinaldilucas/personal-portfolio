<section id="{{resources.works.id}}" data-anchor-scroll="{{resources.works.id}}" class="blocks">
  <div class="blocks__wrapper">
    <h2 class="blocks__title wow animated fadeIn" data-wow-delay="0.1s">{{resources.works.title}}</h2>
    <div class="spacer" data-height="60"></div>
    <div class="blocks__content">
      {{#each portfolio}}
        <div class="blocks__item wow animated fadeIn" data-wow-delay="0.4s">
          <a class="blocks__item-image" title="{{name}}" data-portfolio-anchor data-disclaimer="{{disclaimer}}" data-image="{{id}}">
            <div class="blocks__item-details">
              <h4 class="blocks__item-title">{{name}}</h4>
            </div>
            <div class="blocks__item-thumb">
              <img loading="lazy" src="{{src}}" alt="{{name}}" title="{{name}}" />
              <div class="blocks__item-mask"></div>
            </div>
          </a>
        </div>
      {{/each}}
    </div>
  </div>
</section>
